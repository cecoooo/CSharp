using System;
using System.Collections.Generic;

namespace Santase
{
    internal class StartUp
    {
        static void Main(string[] args)
        {
            // create players
            // input goal;
            // start a game
            // Game class logic:
            /* while(WinningPlayer.count < goal){
                random says who is first
                while(NoPlayerWithEnoughPoints || PlayersStillHaveCards)
                {
                    first play
                    second responce
                    sum the points and set them to the winner
                    check if winner points are enough to stop the game
                    check if cards are over
                    - if not itterate again, this time winner is first
                }
                 */

            /*
            count how many games winner will get (depends on loser's points)
            increase its count
        }
         */
        }
    }
}